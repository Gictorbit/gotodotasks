version: v1
lint:
  use:
    - BASIC
  except:
    - PACKAGE_SAME_DIRECTORY
    - PACKAGE_DIRECTORY_MATCH
    - PACKAGE_VERSION_SUFFIX
    - ENUM_ZERO_VALUE_SUFFIX
    - ENUM_NO_ALLOW_ALIAS
breaking:
  use:
    - WIRE_JSON